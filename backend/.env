# Template dummy .env file for Onsite Safety Detection Backend - change these values for your deployment

# MongoDB connection URI
MONGO_URI=mongodb://localhost:27017/onsite_db

# JWT secret for token generation
JWT_SECRET=your_jwt_secret_key_333333333

# Port for the Express server (optional, defaults to 8081 if not specified)
PORT=8081

